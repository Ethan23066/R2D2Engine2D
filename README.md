# R2D2Engine
R2D2Engine2D is a 2D game engine currently in development and still unstable.
It uses a C++ core, Cython bindings, a Python 3.14 API, and C for GLAD.
The engine relies on GLFW (installed via pip) for window creation and OpenGL context management.
